{% extends 'base.html.twig' %}


{% block title %}
{{'action_return'|trans}}
{% endblock %}

{% block body %}
    <div class="container">
    <div class="card border-0 shadow my-5">
    <div class="card-body p-5">
<h1 class="text-center"> {{ 'action_return'|trans }}</h1>

    <div class ="invisible">
    {{ form_start(form, {method: 'DELETE', action: url('borrowing_return', {id: borrowing.id}) }) }}
    {{form_widget(form)}}
    </div>
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{'action_return'|trans}}" class="btn btn-info" >
    </div>

    <div class="form-group row float-sm-left">
        <a class="btn btn-outline-primary" href="{{ url('category_index') }}" title="{{ 'action_back_to_list'|trans }}">
        {{ 'action_back_to_list'|trans }}
        </a>
    </div>

    {{ form_end(form) }}
    </div>
    </div>
    </div>
{% endblock %}