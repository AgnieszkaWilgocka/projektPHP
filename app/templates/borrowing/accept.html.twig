{% extends 'base.html.twig' %}


{% block title %}
{{'manage_order'|trans}}
{% endblock %}


{% block body %}
    <div class="container">
    <div class="card border-0 shadow my-5">
    <div class="card-body p-5">
<h1>{{'manage_order'|trans}}</h1>

    {{ form_start(form, {method: 'PUT', action: url('borrowing_accept', {id: borrowing.id}) }) }}
    {{ form_widget(form) }}


    <div class="form-group row float-sm-right">
        <input type="submit" value="accept" class="btn btn-primary" >
    </div>

    <div class="form-group row float-sm-left">
        <a href="{{ url('manage_borrowings') }}" title="{{ 'action_back_to_list'|trans }}">
            {{ 'action_back_to_list'|trans }}
        </a>
    </div>

    {{ form_end(form) }}

    </div>
    </div>
    </div>
{% endblock %}