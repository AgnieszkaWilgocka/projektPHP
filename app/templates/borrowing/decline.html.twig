{% extends 'base.html.twig' %}


{% block title %}
Decline
{% endblock %}


{% block body %}
<h1>Decline</h1>

    {{ form_start(form, {method: 'PUT', action: url('borrowing_decline', {id: borrowing.id}) }) }}
    {{ form_widget(form) }}

    <div class="form-group row float-sm-right">
        <input type="submit" value="decline" class="btn btn-primary" >
    </div>

    <div class="form-group row float-sm-left">
        <a href="{{ url('manage_borrowing') }}" title="{{ 'action_back_to_list'|trans }}">
           {{ 'action_back_to_list'|trans }}
        </a>
    </div>

    {{ form_end(form) }}
{% endblock %}