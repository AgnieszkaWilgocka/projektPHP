{% extends 'base.html.twig' %}

{% block title %}
{{'action_create'|trans}}
{% endblock %}

{% block body %}
<h1>{{'action_create'|trans}}</h1>

    {{ form_start(form,  {method: 'POST', action: url('category_create')}) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="save" class="btn btn-primary" > {#przycisk do wysyłania#}
    </div>

    <div class="form-group row float-sm-left">
        <a href="{{ url('category_index') }}" title="{{ 'action_back_to_list'|trans }}">
        {{ 'action_back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}




{% endblock %}