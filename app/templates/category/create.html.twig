{% extends 'base.html.twig' %}

{% block title %}
{{'action_create'|trans}}
{% endblock %}

{% block body %}
    <div class="container">
    <div class="card border-0 shadow my-5">
    <div class="card-body p-5">
<h1>{{'action_create'|trans}}</h1>

    {{ form_start(form,  {method: 'POST', action: url('category_create')}) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{'action_create'|trans}}" class="btn btn-primary" >
    </div>

    <div class="form-group row float-sm-left">
        <a class="btn btn-outline-primary" href="{{ url('category_index') }}" title="{{ 'action_back_to_list'|trans }}">
        {{ 'action_back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}

    </div>
    </div>
    </div>


{% endblock %}